// declarando as variáveis
const got = "Game of Thrones"
const numeroTemporadas = 8

// exibindo o valor das variáveis
console.log (got)
console.log (numeroTemporadas)

// exibindo o tipo das variáveis
console.log (typeof (got))
console.log (typeof (numeroTemporadas))

let novaVariavel //undefined por que não tem nada atribuido
console.log (novaVariavel)
novaVariavel = 22
console.log (novaVariavel)
novaVariavel = undefined
console.log (novaVariavel)

let minhaVariavel = null // recebe o valor nulo/vazio
console.log (minhaVariavel)